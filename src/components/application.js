import React from "react";
import PizzaCard from './pizzaCard';
import Grid from "@material-ui/core/Grid";

const Application = ({numOfPizzas}) => (
  <div>
    <Grid>
    
    </Grid>
  </div>
);
export default Application;
